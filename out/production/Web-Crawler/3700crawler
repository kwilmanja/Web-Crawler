#!/bin/bash

# Need classpath to be explicitly mentioned to include required jar files
CLASSPATH=.
# Invoking the entrypoint file mainfile
# $@ represents all the command-line arguments passed to the script when it is executed.
java -classpath "$CLASSPATH" Crawler.java "$@"

chmod a+x 3700crawler